---
id: TASK-00042
order: 42
ticket: TICKET-00011
title: Reserve Super Admin Role identity and preserve User Role changes
status: done
blocked_by:
---

# Reserve Super Admin Role identity and preserve User Role changes

## What to Build

Reserve the exact `ROLE_SUPER_ADMIN` name for the authoritative managed Role across custom creation, rename,
reconstruction, assignment, and effective-principal resolution. Remove actor-only authorization dependencies from
custom-Role lifecycle and User Role-assignment handlers while preserving ordinary User Role behavior. The
custom-Role membership handlers still use `RoleAdministrationAuthorization` until [TASK-00043](00043-TASK.md)
removes their final dependency.

## Use Case and Effects

| Actor and trigger | Commands / queries | Events | Effect and result |
| --- | --- | --- | --- |
| A consumer creates, renames, or removes a custom Role | `CreateCustomRole`, `RenameCustomRole`, `RemoveCustomRole`; existing Role reads | Existing custom-Role Events on real changes | Exact reserved-name creation/rename fails; ordinary custom-Role lifecycle continues and managed Roles stay runtime-immutable. |
| A consumer assigns or removes a User Role | `AssignRoleToUser`, `RemoveRoleFromUser`; existing User/Role reads | `RoleAssignedToUser`, `RoleRemovedFromUser` after real commits | The managed Super Admin Role follows ordinary association, revision, reference, and no-op behavior, including assignment to a pending User for bootstrap. |
| A User authenticates and receives effective authority | Existing principal resolution | N/A | A custom or inconsistent Role cannot appear as managed `ROLE_SUPER_ADMIN`; only active Users authenticate under existing lifecycle policy. |

## Permissions, Validation, and Compatibility

- The managed flag and exact name together identify the designated Role; no Role ID, duplicate name, stale managed
  definition, or adapter-supplied custom Role qualifies by itself. Invalid reconstruction or assignment fails closed
  without persistence change or success Event.
- The application builder owns caller checks for all Role lifecycle and User Role command entry points, including
  no-ops, and decides assignment/removal authority separately, confirmation, audit, last-admin protection, and
  recovery. Actor IDs in existing Commands remain provenance only. The package adds no named Permission check.
- Remove `RoleAdministrationAuthorization` from create/rename/remove handlers and
  `UserRoleAssignmentAdministrationAuthorization` from assign/remove handlers. Retain the Role administration
  interface only while membership handlers still consume it; [TASK-00043](00043-TASK.md) owns its final removal.
  Ownership-sensitive session, email-change, and invitation checks stay in place.
- Preserve User Role expected revisions, authoritative Role references, transaction rollback, safe failure evidence,
  post-commit success Events, and storage on pending, active, disabled, or deleted Users. This is a breaking pre-1.0
  constructor/composition change, so update maintained consumer guidance and changelog without claiming adoption.

## Acceptance Criteria

- [x] Custom Role creation, rename, and reconstruction reject exact `ROLE_SUPER_ADMIN` impersonation; inconsistent
      stored Role identity cannot elevate a User through assignment or principal reconstruction.
- [x] Ordinary User Role assign/remove, pending-User Super Admin bootstrap, revisions, no-ops, rollback, and event
      ordering retain their existing behavior; active-only authentication remains intact.
- [x] Custom-Role lifecycle and User Role handlers no longer invoke actor-only authorization ports. Narrow
      ownership-sensitive ports remain, and the still-needed custom-Role membership port is left for TASK-00043.
- [x] Focused package tests cover both accepted and rejected Role/User paths, including stale Role references and
      safe failure outcomes.

## Verification and Evidence Plan

- Focused Role Domain, custom-Role identity handler, User Role assignment, and principal-resolution tests.
- `./bin/planning-check --write`, `./bin/planning-check`, and the canonical `./bin/build` with exact coverage.
- No visual UI exists in this package. Retain before/after command and principal behavior evidence plus the complete
  build receipt/log in this TASK's ignored `.runs/` space when implemented.

## Implementation checkpoint

Implementation is on `feature/task-00042-super-admin-role` in the main checkout, based on clean `develop`
`02abbd5f2fcc0b3a61428868f6080ce0e8611352`. At the pre-review checkpoint, there is no PR, merge,
release, or consumer adoption. The application builder is responsible for every caller check, including no-ops.
Review acceptance was pending at that checkpoint.

### Implementation and nonvisual proof

- `Role` rejects reserved-name custom reconstruction and rename, and a final identity guard validates the selected
  Role against the authoritative name lookup before assignment, removal, or principal publication. The repository
  contract requires unique ID/name indexes and rejects ambiguous results; consumer adapters must implement this
  fence. The package does not supply or certify a production adapter.
- Removed the actor-only authorization dependencies from three custom-Role lifecycle and two User Role handlers,
  retiring only the User Role port and its exception. Custom-Role membership authorization remains for TASK-00043;
  ownership-sensitive checks are unchanged. Updated consumer migration guidance, changelog, and `CONTEXT.md`.
- Before/after is nonvisual: existing commands previously required an actor-only port and could construct a custom
  reserved Role; current executable tests show custom creation/reconstruction rejection, pending managed-role
  bootstrap, ordinary assign/remove, no-ops, reference rollback, safe failure events, and principal fail-closed
  behavior. No UI or useful screenshot exists in this framework-neutral library.
- Focused container PHPUnit: 55 tests / 625 assertions, 27 tests / 75 assertions after subtype coverage,
  and 9 tests / 214 assertions for the final User Role paths. Final canonical `./bin/build`: **exit 0**,
  590 tests / 4553 assertions, exact coverage 5091/5091 statements;
  Composer validation, PHP syntax, planning, PHPCS, PHPStan, architecture, Rector, PHPUnit, coverage, and
  documentation links passed. Final complete log, exit result, UTC start/end, and tested input fingerprints:
  `.runs/2026-09-26-task-00042/` (`build.log`, `build-exit.txt`, `receipt.md`). Earlier full-gate attempts failed
  PHPCS (attempts 1, 3 and 6) and Rector dry-run (attempt 2); attempts 4 and 5 passed, but preceded the final
  test-only state-coverage change; the final passing gate is attempt 7. No warnings, deprecations, skips, or
  omissions in the final gate. Hosted CI not checked.
- At this pre-review checkpoint, the remaining risk is consumer adapter uniqueness/transaction fencing and builder
  authorization adoption, outside this package's verified boundary. Independently review the current commit before
  any publication or landing.

## Landing checkpoint

Independent review **accepted** the clean implementation at `faef68d7ef9cf0cfaba814f5bb7a227b37835847`
against `develop` `02abbd5f2fcc0b3a61428868f6080ce0e8611352`; the canonical handoff is
`.runs/reviews/TASK-00042/review.md`. All four acceptance criteria were covered and there were no blocking or
non-blocking findings requiring disposition. Review noted consumer-owned repository uniqueness/transaction fences
and command-entry authorization as residual risks, not package defects. This TASK is done on accepted implementation
and local verification, not on PR approval, merge, release, or consumer adoption. The base has not advanced, so no
implementation reconciliation is needed. Landing checks, publication identity and any PR URL are recorded below
when verified; the original review remains anchored to the reviewed OIDs. The review report describes the package
as private for hosted-check policy; hosting currently reports the repository **public**. That policy inference is
not relied on for acceptance: no required hosted check or protected `develop` branch is declared here, and hosted
CI is not claimed to have passed. The independent behavioral acceptance and local proof remain applicable.

Before first publication, landing reran Docker focused PHPUnit (**43 tests, 375 assertions**),
`./bin/planning-check` (**63 records, 9 active**), `./bin/documentation-check`, and `git diff --check`, all passed.
The fresh `./bin/build` exited **0** (590 tests, 4553 assertions, exact 5091/5091 statements); full log, start/end,
and actual exit are in `.runs/2026-09-26-task-00042/land-prepublish-*`. No gate warnings, deprecations, or skips
were reported. These completion/status/progress updates and generated views change planning prose only; the
reviewed implementation and its build inputs remain unchanged.

## Dependencies and Boundaries

No unfinished TASK prerequisite. [TASK-00043](00043-TASK.md) completes Role port removal; the managed-policy
integration in [TASK-00045](00045-TASK.md) relies on this Role identity rule. Consumer confirmation/audit,
last-admin logic, Agent OS implementation, tag, and release remain outside this TASK. Accepted requirements:
[TICKET-00011](../tickets/00011-TICKET.md), [WF-013](../wayfinder/tickets/WF-013-target-aware-role-administration.md),
and [WF-014](../wayfinder/tickets/WF-014-super-admin-role-elevation.md).
