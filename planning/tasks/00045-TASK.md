---
id: TASK-00045
order: 45
ticket: TICKET-00010
title: Reconcile protected managed policy without forbidden membership
status: in-progress
blocked_by: TASK-00042, TASK-00043, TASK-00044
---

# Reconcile protected managed policy without forbidden membership

## What to Build

Reject a managed definition that attaches a `SUPER_ADMIN_ONLY` Permission outside authoritative managed
`ROLE_SUPER_ADMIN`. Reject promotion of an existing managed Permission while any custom Role, ordinary managed Role,
or Agent still holds it. Integrate the Role and Agent tier fences from the prerequisite TASKs with reconciliation
so a grant and promotion cannot both succeed under controlled concurrent or stale-state interleavings.

## Use Case and Effects

| Actor and trigger | Commands / queries | Events | Effect and result |
| --- | --- | --- | --- |
| A consumer supplies managed policy | `ReconcileManagedPolicy`; existing `PreviewManagedPolicy` remains read-only | `ManagedPolicyReconciled` after successful commit | A protected Permission may appear only on authoritative managed `ROLE_SUPER_ADMIN`; invalid definitions fail before policy mutation. |
| A consumer promotes an existing managed Permission | `ReconcileManagedPolicy`; authoritative Role/Agent membership reads | Existing failure Event on rejection; no success Event | Forbidden custom-Role, ordinary managed-Role, or Agent membership blocks the entire promotion. No membership is automatically removed. |
| Grant and promotion interleave | Existing custom-Role or Agent grant plus `ReconcileManagedPolicy` | Success Event only for committed operation | Grant-first blocks promotion while membership remains; promotion-first blocks grant. Losing operation has no partial write. |

## Permissions, Validation, and Compatibility

- Consumer-managed policy declares Permission names and tiers. The application builder protects reconciliation and
  grant entry points; this TASK adds no caller authorization, scope check, HTTP response, or protected-name list.
- Validate the managed flag and exact `ROLE_SUPER_ADMIN` name together. Malformed references, duplicate/mismatched
  Role identity, and forbidden protected membership fail closed before reconciliation changes authority.
- Promotion reads authoritative Role and Agent memberships and preserves the checked state through the same Unit of
  Work as tier change. Repository contracts and in-memory implementations must make the grant/promotion conflict
  outcome explicit; the concrete persistence-adapter lock method is consumer-owned.
- Preserve exact managed-policy reconciliation, stable IDs, existing no-op behavior, rollback, safe failure Event,
  and post-commit `ManagedPolicyReconciled` publication. Document pre-1.0 contract implications without claiming a
  tag, Agent OS adoption, or historical data migration.

## Acceptance Criteria

- [x] Managed-policy definition/preview and reconciliation reject a protected Permission on an ordinary managed
      Role or identity-spoofing Role before any authority change.
- [x] Promotion with custom-Role, ordinary managed-Role, or Agent membership fails atomically with no success Event;
      after those memberships are removed, a valid policy can reconcile.
- [x] Focused package unit tests prove grant-first and promotion-first conflict outcomes for custom Role and Agent,
      including stale or idempotent grant paths, with no forbidden committed membership or partial policy update.
- [x] Existing exact reconciliation, safe failure publication, original exception rethrow, and post-commit success
      ordering remain proven; maintained package docs describe the repository contract.

## Verification and Evidence Plan

- Focused ManagedPolicy definition/planner/handler tests and controlled Role/Agent grant-promotion interleavings.
- `./bin/planning-check --write`, `./bin/planning-check`, and the canonical `./bin/build` with exact coverage.
- Use transaction/interleaving tests as nonvisual before/after evidence; retain the complete build receipt/log in
  this TASK's ignored `.runs/` space when implemented. Package unit proof does not claim PostgreSQL adapter proof.

## Dependencies and Boundaries

TASK-00042 supplies authoritative Role identity; TASK-00043 and TASK-00044 supply Role and Agent grant eligibility
and tier-aware reference contracts. This TASK owns their cross-path integration proof and TICKET-00010 acceptance.
Consumer locking/schema/HTTP tests, Agent OS cleanup migration, release certification, tag, and publication are
outside scope. Accepted requirements: [TICKET-00010](../tickets/00010-TICKET.md),
[WF-015](../wayfinder/tickets/WF-015-forbidden-authority-remediation.md), and
[WF-016](../wayfinder/tickets/WF-016-atomic-enforcement-and-proof.md).

## Implementation checkpoint (2026-09-26, before independent review)

On `feature/task-00045-protected-managed-policy` from `develop` (`adbc33d`), the policy definition rejects
protected membership outside managed `ROLE_SUPER_ADMIN`. The planner rejects forbidden promotion in preview and
apply and checks designated Role identity; the Permission write rechecks the same authoritative Role/Agent reference
state behind the grant fence. In-memory repositories enforce and exercise grant-first, promotion-first, and
post-preflight stale interleavings, including rollback of an earlier policy write. Existing Role/Agent no-op and
stale-grant tests remain part of the full suite. Updated `CONTEXT.md` and
[consumer migration guidance](../../docs/permission-tier-v0.4-migration.md) describe the pre-1.0 contract and
required adapter/builder wiring. The new Agent repository read is mandatory for actual promotion: an older planner
composition without it rejects promotion closed.

Focused managed-policy and Role tests passed (29 tests, 356 assertions at the last focused checkpoint).
`./bin/planning-check --write`, `./bin/planning-check`, `git diff --check`, and the complete `./bin/build`
passed. The full gate ran on the implementation tree before this prose-only evidence update: 603 tests,
4669 assertions, exact 5126/5126 statement coverage, no reported warnings, skips, or deprecations; PHPCS,
PHPStan, deptrac, Rector dry-run and documentation checks passed. Full log and independently captured exit 0:
`.runs/2026-09-26-task-00045/build.log`, `build.exit`, `build-start.txt`, `build-end.txt` (ignored).
No PostgreSQL adapter lock or consumer integration is claimed. At this checkpoint there is no PR, review verdict,
release, or deployment; independent review is next. Keep TASK `in-progress` until accepted.
