---
id: TASK-00037
order: 37
ticket: TICKET-00007
title: Model recoverable credential-delivery state and repository contracts
status: done
blocked_by:
---

# Model recoverable credential-delivery state and repository contracts

## What to Build

Revise grant-owned activation, password-reset, and email-change delivery entities and repository contracts so the
package can discover due work and safely persist claims, leases, retries, permanent outcomes, expiry, and stale
expected-state rejection without duplicate delivery state. Replace public ciphertext exposure with the narrow
encrypted-material boundary needed by later package orchestration.

## Acceptance Criteria

- [x] Each delivery generation retains its stable ID and gains package-owned retry due time, claim token/lease, attempt
      metadata, typed safe status, and outcome transitions consistent with its owning grant/revision.
- [x] Repository contracts provide deterministic secret-free due-work discovery and enforce complete predecessor/next
      state comparisons for claim, lease recovery, and expected outcome updates through the caller's Unit of Work.
- [x] Grant replacement, consumption, revocation, cancellation, and expiry invalidate stale delivery work and destroy
      ciphertext where required.
- [x] Domain/repository tests prove rollback, claim races, stale revisions, lease expiry, retry-until-expiry, permanent
      terminalization, and no secret-bearing safe view or serialized message.

## Verification

- Focused Domain and repository tests
- `./bin/planning-check`
- `./bin/build`

## Completion Notes

Completed at the 2026-09-24 implementation checkpoint. Activation, password-reset, and email-change delivery
entities now share a package-owned recoverable lifecycle with opaque claim leases, bounded retry scheduling, safe
outcome metadata, terminal material destruction, and subtype-preserving grant transitions. Their repository
contracts expose deterministic secret-free due-work discovery; the in-memory reference adapters enforce complete
predecessor/next-state comparisons and participate in Unit of Work rollback.

Focused lifecycle and repository tests passed. `./bin/planning-check`, `git diff --check`, and the complete
`./bin/build` gate passed with 564 tests, 4,383 assertions, and exact 4,704/4,704 statement coverage. The build also
confirms zero PHPStan errors after the explicitly authorized ancillary cleanup of pre-existing redundant-assertion
and duplicate-array-key findings.

Review revision 1 resolves both blocking findings. Live-claim validation now rejects material access and delivered,
retryable, or permanent outcomes before `claimedAt`. The email-change reference repository now rejects duplicate
grant and delivery identifiers on initial add and applies the complete pristine ownership/expiry checks. Regression
tests first failed for both defects, then passed. The final revision `./bin/build` passed with 566 tests, 4,388
assertions, and exact 4,706/4,706 statement coverage; PHPCS and Rector findings from the first two build attempts were
corrected before the passing run.
