---
id: TASK-00041
order: 41
ticket: TICKET-00009
title: Classify every Permission with a non-null tier
status: done
blocked_by:
---

# Classify every Permission with a non-null tier

## What to Build

Make `Permission` and its safe administrative read result expose one non-null `PermissionTier`. A custom Permission
starts `ADMIN_SAFE` and cannot be made protected through its runtime-owned API; managed definitions keep their
consumer-declared tier. Update affected package fixtures and in-memory repositories. This is the shared contract
needed by the custom-Role and Agent assignment TASKs.

## Use Case and Effects

| Actor and trigger | Commands / queries | Events | Effect and result |
| --- | --- | --- | --- |
| A consumer defines a custom Permission | Existing Domain `Permission::define` API; no Command or Query | N/A | New custom authority has explicit `ADMIN_SAFE` tier and stable existing identity/name/timestamps. |
| A consumer reconciles a managed Permission definition | Existing `ReconcileManagedPolicy` path | Existing managed-policy Event after commit | The managed Permission retains the exact declared `ADMIN_SAFE` or `SUPER_ADMIN_ONLY` tier; this TASK does not decide membership. |
| An administrator reads a Permission | `GetPermissionById`, `ListPermissions`, `PermissionView` | N/A | Aggregate and safe result expose the same non-null tier and existing managed flag. |

## Permissions, Validation, and Compatibility

- Tier eligibility does not authorize a caller. The application builder protects command entry points; this TASK
  adds no actor or target authorization port.
- Invalid or unexpected null-tier reconstruction fails closed. No custom API may create or reclassify a
  `SUPER_ADMIN_ONLY` Permission. Managed classification remains consumer-owned.
- Update public aggregate/read signatures and serialization consistently, including any maintained documentation
  and tests. This is a breaking pre-1.0 package contract for consumer persistence and projections; Fight Agent OS
  has no stored Permissions to migrate. Consumer schema/adapters are outside this TASK.
- Preserve existing identity, uniqueness, timestamps, read-query purity, and managed reconciliation behavior.

## Acceptance Criteria

- [x] Custom Permission creation produces `ADMIN_SAFE`; managed construction/reconciliation retains its declared
      non-null tier; malformed null authority cannot be silently read as eligible.
- [x] `Permission`, `PermissionView`, `GetPermissionById`, and `ListPermissions` expose consistent non-null tier
      contracts and canonical safe array values; package fixtures and in-memory repositories follow the contract.
- [x] Focused Domain and query tests prove custom/managed tiers and rejection of invalid reconstruction without
      changing unrelated Permission behavior.
- [x] Consumer-facing migration notes explain the non-null contract without claiming Agent OS schema adoption or a
      package release.

## Verification and Evidence Plan

- Focused Permission Domain and Permission query tests, including safe serialization and malformed-data rejection.
- `./bin/planning-check --write`, `./bin/planning-check`, and the canonical `./bin/build` with exact coverage.
- This framework-neutral contract has no useful screenshot. Retain focused before/after behavior evidence and the
  complete build receipt/log in this TASK's ignored `.runs/` space when implemented.

## Implementation and verification checkpoint

At the pre-review checkpoint, `Permission::define()` creates an `ADMIN_SAFE` custom Permission and the protected
constructor rejects a protected custom tier; both aggregate and view tier types are non-null. Existing in-memory
repositories retain the typed aggregate and need no persistence adapter change. Managed tier definitions and
reconciliation still use the consumer-supplied tier. See [the consumer migration note](../../docs/permission-tier-v0.4-migration.md)
for projection, hydration, and null-data handling. No Agent OS schema adoption or release is claimed.

- Baseline focused Permission/query tests: 6 tests, 44 assertions, exit 0; existing custom view had `tier: null`.
  After implementation: 10 tests, 53 assertions, exit 0; custom view has `tier: ADMIN_SAFE` and null hydration is
  rejected. Logs: `.runs/2026-09-26-task-00041/baseline-focused.log` and `focused.log`.
- First `./bin/build` exited 2 at PHPCS for a 127-character test line; fixed the line and reran. First attempt:
  `.runs/2026-09-26-task-00041/build-first-phpcs-failure.log` and its receipt.
- Complete rerun `./bin/build` exited 0 on PHP 8.5.7 in the project Docker container: 587 tests, 4572 assertions,
  exact statement coverage 5067/5067; Composer validation, PHP syntax, planning, PHPCS, PHPStan, architecture,
  Rector dry run, PHPUnit, exact coverage, and documentation links passed. Log and process exit receipt:
  `.runs/2026-09-26-task-00041/build.log` and `build-receipt.txt`. No gate warnings, skips, or deprecations reported.
  `./bin/planning-check --write`, `./bin/planning-check`, and `git diff --check` also passed.
- The full gate tested the implementation, tests, README, migration note and generated planning views before this
  evidence-only TASK prose update. No executable or build inputs changed afterward; the current planning and
  documentation views receive targeted checks before commit. Consumer persistence changes and protected-membership
  enforcement remain separate. Branch `feature/task-00041-permission-tier` remains in progress pending independent
  review; no PR, merge, tag, or release is claimed at this checkpoint.

## Landing acceptance checkpoint

At the 2026-09-26 pre-publication landing checkpoint, the independent version-3 report at
`.runs/reviews/TASK-00041/review.md` accepted the change from base
`f9d249a73cd8a52a4fa5f094789f9d6d7045df59` to head
`1a5325b2c5bc027b1f1f22dd3292ee6ce26e9121`: all four TASK criteria have evidence, with no blocking
findings. The earlier review's reviewer-profile process gap is resolved by the current review workflow; the accepted
report notes four separate uncommitted standards-refresh documents, which are not part of this TASK. No base
reconciliation is required: `develop` remains at the reviewed base. The non-blocking reflection-test observation and
consumer schema/adoption and later tier-eligibility work remain as described in the review; no source fix or release
is claimed. `done` means independently accepted implementation and required local verification, not PR approval or
merge. Before initial publication, fresh focused Permission Domain/query tests passed (14 tests, 66 assertions),
`./bin/planning-check --write` and `./bin/planning-check` validated 63 records with 11 active, and
`git diff --check` passed. A fresh `./bin/build` passed (587 tests, 4572 assertions, exact 5067/5067 statement
coverage), including Composer, syntax, PHPCS, PHPStan, architecture, Rector and documentation links; no gate
warnings, skips or deprecations were reported. The run log and exit receipt are in
`.runs/2026-09-26-task-00041/land-pre-publish-build.log` and `land-pre-publish-build-receipt.txt`.
Publication and the actual PR URL are recorded separately as they complete.

## Dependencies and Boundaries

No unfinished TASK prerequisite. [TASK-00043](00043-TASK.md) and [TASK-00044](00044-TASK.md) consume this tier
contract. Protected Role/Agent membership, managed-policy promotion, consumer database migration, tag, and release
are outside this TASK. Accepted requirements: [TICKET-00009](../tickets/00009-TICKET.md),
[WF-012](../wayfinder/tickets/WF-012-custom-permission-delegation.md), and
[ADR 0009](../adr/0009-non-null-custom-permission-tier.md).
