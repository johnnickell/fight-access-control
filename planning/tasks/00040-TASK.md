---
id: TASK-00040
order: 40
ticket: TICKET-00008
title: Remove deprecated Fight Common contract dependencies
status: done
blocked_by:
---

# Remove deprecated Fight Common contract dependencies

## What to Build

Migrate the maintained AccessControl package surface from Fight Common's deprecated `UnitOfWork` interface to
`TransactionalUnitOfWork`. Update production constructor dependencies, test doubles and fixtures, repository and
consumer guidance, and compatibility notes without changing established transactional callbacks or observable
business behavior. Compare the final maintained surface once against Fight Common 1.2's authoritative deprecated
API declarations and retain the audit evidence in the TASK's ignored run space.

This TASK is the priority implementation prerequisite for TASK-00038 so subsequent credential-delivery orchestration
is built directly on the supported transaction contract rather than introducing more deprecated dependencies.

## Acceptance Criteria

- [x] Every maintained production constructor and internal collaborator that requires transaction coordination types
      against `TransactionalUnitOfWork`; no production code requires Common `UnitOfWork` or its standalone `commit()`
      method, and no deprecated compatibility bridge is added.
- [x] Test doubles, anonymous fixtures, type assertions, configuration, repository guidance, and current consumer
      documentation use the supported contract while historical planning evidence remains unchanged.
- [x] Existing `commitTransactional()` callback boundaries preserve reads, validation, mutation, persistence,
      reference fencing, audit work, callback results, and post-commit success publication.
- [x] Representative command-handler, authentication-service, Agent-service, and current-Agent-principal tests prove
      successful commit, rollback, callback-result propagation, success-event ordering, existing failure evidence,
      authority fencing, request-local behavior, and unchanged exception identity.
- [x] A run-local one-time audit compares maintained production code, tests, configuration, and current consumer
      guidance with Fight Common 1.2's deprecated declarations and records that no deprecated Common API dependency
      remains.
- [x] Consumer migration guidance and `CHANGELOG.md` classify the constructor dependency change as breaking pre-1.0
      compatibility and identify `TransactionalUnitOfWork` as the replacement without claiming certification, tag,
      publication, consumer upgrade, or deployment.
- [x] Planning records and generated views reflect that TASK-00038 follows this migration and TASK-00039 remains the
      later release-qualification owner.

## Verification

- Focused transaction, handler, service, and principal-resolution tests
- Recorded Fight Common 1.2 deprecated-API comparison under `.runs/`
- `./bin/planning-check --write`
- `./bin/planning-check`
- `./bin/build`

## Completion Notes

At the pre-commit implementation checkpoint on 2026-09-24:

- All 33 transaction-aware production collaborators require Fight Common's `TransactionalUnitOfWork`; the shared
  in-memory test double and four focused anonymous failure fixtures implement the same supported contract without a
  standalone `commit()` method. Existing `commitTransactional()` callback bodies and post-commit publication remain
  unchanged.
- `README.md` and `CHANGELOG.md` document the breaking pre-1.0 constructor migration and explicitly avoid release,
  publication, consumer-upgrade, or deployment claims.
- The Fight Common v1.2.0 audit found 40 authoritative deprecated declarations and zero references from maintained
  production code, tests, configuration, or current consumer guidance. Evidence:
  `.runs/notes/2026-09-24-task-00040/audit/deprecated-common-api-audit.txt`.
- Focused transaction-sensitive verification passed 117 tests with 1,307 assertions across command handling,
  authentication, Agent provisioning and credential lifecycle, current-Agent principal resolution, rollback, and
  callback-result behavior. Logs are under `.runs/notes/2026-09-24-task-00040/focused/`.
- The first two full builds failed on PHPCS line-length warnings introduced while updating repository terminology;
  those warnings were corrected. The final `./bin/build` passed with 571 tests and 4,445 assertions, exact statement
  coverage of 4,716/4,716, zero architecture violations, and valid documentation links. Receipt, complete log, and
  exit evidence: `.runs/notes/2026-09-24-task-00040/build-00003/`.
- Visual screenshots would not usefully prove this framework-neutral constructor-contract migration; executable
  behavioral tests, static analysis, architecture checks, exact coverage, and the recorded API audit provide the
  applicable nonvisual evidence.

At the 2026-09-24 pre-publication landing checkpoint, independent review sequence 1 accepted base
`56c2a643f25c8a822f5acdc7ed6672b71415be5a` through implementation head
`957577859936d684ba7d6eb81735a1304055012b` with 100% Spec and Standards scores. There are no blocking or
non-blocking findings; the review's provisional `SPEC-001` evidence concern was rebutted by reproducing both the
successful-build and final-tree digests with the repository's effective mnemonic diff prefixes.

Fresh landing verification passed: seven representative transaction-sensitive suites ran 115 tests with 1,349
assertions; `./bin/planning-check` validated 54 records with 7 active; `git diff --check develop...HEAD` passed; and
`./bin/build` passed 571 tests with 4,445 assertions and exact 4,716/4,716 statement coverage. Composer validation and
security audit, syntax, PHPCS, PHPStan, architecture, Rector, and documentation links passed without product warnings,
deprecations, skips, or advisories. The initial read-only focused-test invocation could not write its configured
coverage report; the same suites were rerun with the normal writable test mount and completed successfully. The build
updated only ignored local dependency output and emitted its routine funding notice.

Initial branch push and PR creation remain pending at this checkpoint. This canonical base worktree and the required
review/build evidence are retained. No TASK-owned container, volume, process, or database exists; generic ignored
`composer.lock`, `vendor/`, and `var/` output plus the reusable `fight-access-control` image are not classified as
TASK-owned cleanup candidates. Human approval and merge remain required; branch deletion, archive, release,
deployment, and certification are not authorized. Residual risk remains that consumers must update and qualify their
composition bindings, and the accepted deprecation audit is intentionally one-time rather than a recurring gate.
