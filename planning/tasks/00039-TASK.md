---
id: TASK-00039
order: 39
ticket: TICKET-00007
title: Qualify the v0.3.0 credential-delivery contract and migration
status: done
blocked_by: TASK-00038
---

# Qualify the v0.3.0 credential-delivery contract and migration

## What to Build

Complete the consumer-facing migration and release qualification for the revised public delivery contract. Document
shared-connection repository participation, direct handler/provider registration, scheduled discovery, idempotency,
at-least-once recovery, secure diagnostics, and Agent OS's permitted email-change wiring deferral.

## Acceptance Criteria

- [x] Migration guidance identifies every replaced invoker/ciphertext contract, repository obligation, worker
      responsibility, stable idempotency rule, and removed copied/consumer lifecycle policy.
- [x] Contract/conformance evidence proves rollback, restart recovery, no in-transaction provider call, duplicate
      effect identity, competing claims, stale outcomes, abandoned-claim recovery, and secret handling.
- [x] `CHANGELOG.md` classifies the breaking pre-1.0 change as `v0.3.0`; tag, signing, publication, and consumer
      upgrade execution remain explicitly outside this TASK.
- [x] Planning views, local qualification receipt, `./bin/planning-check`, and `./bin/build` establish the release
      handoff without claiming hosted CI, tag, or publication.

## Verification

- Focused contract/migration checks
- `./bin/planning-check --write`
- `./bin/planning-check`
- `./bin/build`

## Completion Notes

At the 2026-09-25 pre-review implementation checkpoint, the new
`docs/credential-delivery-v0.3-migration.md` identifies the removed invitation/email-change invokers, all three
removed raw ciphertext getters, the shared provider and cipher contracts, persisted claim/outcome fields,
complete-state repository CAS and discovery rules, exhaustive family-specific existing-row mappings, required
reconciliation for every potentially invoked v0.2 package or consumer-owned transport path, deterministic `due_at`
sources and terminal fallbacks, shared-connection transaction participation, direct handler/query registrations,
scheduled worker responsibilities, stable provider idempotency identity, at-least-once crash recovery, safe
diagnostics, removed consumer-owned lifecycle policy, and Fight Agent OS's permitted email-change wiring deferral.
README links the guide and
`CHANGELOG.md` classifies both intentional pre-1.0 breaks under the dated `v0.3.0` candidate without claiming a tag,
signing, publication, consumer upgrade, or deployment.

The guide's conformance matrix traces rollback, deterministic restart discovery, out-of-transaction provider calls,
duplicate effect identity, competing claims, stale outcomes, abandoned-lease recovery, and secret handling to
existing executable tests. The final focused qualification slice passed 97 tests with 784 assertions. Three retained
preliminary invocations did not execute tests: the first lacked a non-interactive container mode, the second ran
before the isolated worktree had dependencies, and the third used an unsupported multi-path invocation and emitted
one Xdebug coverage warning. Their receipts remain alongside the passing focused receipt so setup failures are not
misreported as product failures.

The fresh canonical `./bin/build` passed 581 tests with 4,554 assertions and exact 5,064/5,064 statement coverage.
Composer validation and security audit, syntax, planning integrity, PHPCS, PHPStan, architecture, Rector, PHPUnit,
and documentation links passed without product warnings, deprecations, skips, or advisories; Composer emitted its
routine funding notice. The complete build log, independently captured exit code, content identity, and receipt are
under `.runs/notes/2026-09-25-task-00039/build-0001/`; focused evidence is under the sibling `focused-0001` through
`focused-0004` directories.

Review sequence 1 requested three bounded documentation corrections. That revision limits asynchronous queue dispatch
to one bounded discovery page per scheduling cycle, permits drain-until-short paging only after synchronous handlers
complete, inventories `PasswordResetDelivery::getCiphertext()` with the other removed ciphertext getters, and cites
the lifecycle test that directly proves due-retry eligibility.

Review sequence 2 identified two further migration gaps. The current guide quarantines every potentially invoked v0.2
invitation/email-change `pending`, `claimed`, or `failed` row until provider reconciliation, deduplication seeding, safe
terminalization, or an explicit controlled-duplicate decision supplies its disposition. It separately maps every
family's recoverable and terminal shapes, including status-preserving v0.2 invalidation and ambiguous ciphertext-free
password-reset rows, without recreating material or carrying an active status into v0.3 without material. The focused
migration-contract probe, documentation links, `planning-check --write`, `planning-check`, executable-input
equivalence check, and `git diff --check` pass for this revision; detailed evidence is retained under
`.runs/notes/2026-09-25-task-00039/revision-0002/`.

Review sequence 3 found that v0.2 password-reset ciphertext could have been submitted by a consumer-owned transport
before its separate confirmation committed and that terminal row mappings omitted required `due_at` state. The guide
now quarantines recoverable password-reset rows unless trustworthy history proves no submission, applies the same
accepted-effect, deduplication, invalidation, or controlled-duplicate dispositions to every uncertain historical
effect, and specifies deterministic `due_at` values for every active and terminal mapping. Current source-backed
migration, documentation, planning, input-equivalence, and diff evidence is retained under
`.runs/notes/2026-09-25-task-00039/revision-0003/`.

Review sequence 4 identified three remaining migration-contract gaps. The guide now requires a stable-snapshot
no-bridge cutover that stops and drains every affected v0.2 originating, replacement/retry, transport/confirmation,
consumption, expiry, cancellation/revocation, terminal, worker, and subscriber path before row classification, then
resumes only compatible v0.3 writers after schema, adapter, binding, code, and row validation. It permits encrypted
material reveal only within the approved persistence or committed-claim cipher/decryption boundaries. Exact history,
proved-never-attempted rows, and incomplete history now receive deterministic attempt-count and nullable-metadata
rules; uncertain replay uses the package-owned baseline of one, whose next v0.3 claim and retryable outcome produce
the documented 120-second backoff without claiming an exact historical count. The final source-backed migration
probe passed 31 checks covering the named v0.2 handler-file inventory, the reveal contract, attempt-count behavior,
and preservation of prior corrections. Three preliminary probe runs are retained as setup failures: one used the
wrong status-view path, and two used whitespace-sensitive source assertions; none completed the probe or found a
product defect. Documentation links, `planning-check --write`, `planning-check`, executable/build-input equivalence,
and `git diff --check` passed for that revision. Detailed evidence is retained under
`.runs/notes/2026-09-25-task-00039/revision-0004/`.

Review sequence 5 found that `EncryptedCredentialMaterial` itself exposed ciphertext through ordinary diagnostics and
serialization, that the destructive cutover could restore application/schema without restoring row material or
provider deduplication state, and that the sequence-4 writer-probe description exceeded its evidence. The repaired
boundary stores ciphertext in `SensitiveParameterValue`, marks construction inputs sensitive, redacts `print_r()` and
`var_dump()`, prevents `var_export()` traversal, and rejects direct or containing-delivery serialization while keeping
explicit `reveal()` for approved persistence and committed-claim cipher operations. The regression first failed as
expected with 12 tests, 70 assertions, and one failure, then passed with 12 tests and 75 assertions. One preliminary
runner attempt failed before tests because the container required a TTY. A broader first focused run exposed three
obsolete assertions that serialized recoverable deliveries; after those tests inspected ordinary diagnostics instead,
the focused contract slice passed 98 tests with 791 assertions.

The migration guide now requires either a genuinely rollback-capable atomic transformation or a verified restorable
application/schema/data/material/provider-state snapshot and generation-keyed source ledger. It orders reversible
provider deduplication staging after database validation, defines pre-activation rollback and roll-forward, marks
post-activation effects as a new reconciliation boundary, and forbids either version's writers from resuming until the
whole state validates coherently. The revised source-backed probe passed 50 checks, including mutation signatures from
all named v0.2 handlers and `AuthenticationService` activation, reset, and email-change consumption paths, prior
corrections, encrypted-material protection, and migration recovery.

The fresh sequence-5 `./bin/build` passed 582 tests with 4,561 assertions and exact 5,066/5,066 statement coverage.
Composer validation and security audit, syntax, planning integrity, PHPCS, PHPStan, architecture, Rector, PHPUnit,
and documentation links passed without product warnings, deprecations, skips, or advisories; Composer emitted only
its routine funding notice. Detailed red/green, focused, migration-probe, and complete-gate evidence is retained under
`.runs/notes/2026-09-25-task-00039/revision-0005/`.

No screenshot would usefully prove this framework-neutral migration contract. The linked executable tests, exact
coverage, source-backed migration probe, documentation-link check, planning validation, and local receipts provide the
applicable nonvisual evidence. Hosted CI is not checked by this build. Independent re-review remains required; commit
publication, PR creation, merge, tag signing, release/publication, consumer upgrade, deployment, archive, and cleanup
are not authorized by this checkpoint.

At the 2026-09-25 landing-finalization checkpoint, independent review sequence 6 accepted the exact clean range
`a889db9dda5e01f2a3ea47bb0ce7dadca8b253ca..51b1542c3e37ae258d07bfe853c309cb6ce77951`
with 100% Spec and Standards scores, no blocking or non-blocking findings, and explicit resolution of F-001 through
F-013. The canonical report is retained at `.runs/reviews/TASK-00039/review.md` with immutable sequence 6 history.

Fresh pre-finalization verification on accepted head `51b1542c3e37ae258d07bfe853c309cb6ce77951` passed the focused
credential-delivery slice with 98 tests and 791 assertions, the source-backed migration probe with 50 checks,
`./bin/planning-check` with 54 records and 6 active, range `git diff --check` with no output, and `./bin/build` with 582
tests, 4,561 assertions, and exact 5,066/5,066 statement coverage. Composer validation/security audit, syntax,
planning, PHPCS, PHPStan, architecture, Rector, PHPUnit, coverage, and documentation links passed without product
warnings, deprecations, skips, or advisories; Composer emitted its routine funding notice for 53 packages. The first
landing focused invocation could not attach stdin to the wrapper's TTY-enabled container and executed no tests; the
PTY-backed rerun and every subsequent required check passed. Evidence is retained under
`.runs/notes/2026-09-25-task-00039/landing/`.

Publication is pending at this checkpoint: the feature branch has not yet been pushed, no pull request exists, and
hosted CI is not claimed. Planned post-publication cleanup is limited to the registered isolated TASK worktree and its
previewed ignored `composer.lock`, `vendor/`, and `var/` build outputs; no TASK-specific container, volume, network,
process, or database exists. The base worktree, local feature branch, implementation/build/review evidence, and all
ambiguous or shared resources remain preserved.

After the authoritative TASK status and generated planning views changed, the required final rerun passed the same
focused 98 tests/791 assertions and migration 50 checks, `./bin/planning-check` validated 54 records with 5 active,
`git diff --check` produced no output, and `./bin/build` again passed 582 tests/4,561 assertions with exact
5,066/5,066 statement coverage and the same warning-free product result. This final-tree evidence is retained under
`.runs/notes/2026-09-25-task-00039/landing/post-authoritative-0001/`.

Residual risk remains consumer-owned: each consumer must prove shared-connection atomicity, complete-state CAS, safe
cipher/persistence boundaries, backup/key recovery, historical-effect reconciliation, provider deduplication, and its
maintenance-window cutover. Human approval and merge, branch deletion, archive, release certification, tag signing,
publication, consumer upgrade, and deployment remain separate actions.
